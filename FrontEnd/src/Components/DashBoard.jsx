import { useNavigate } from "react-router-dom"

export function DashBoard () {
  const navigate = useNavigate();
    return <>
       <div>
          Hii there u have successfully reached dashboard page
       </div>
    </>
}